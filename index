<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Couple Workout</title>
    <script src="https://unpkg.com/react@18/umd/react.production.min.js"></script>
    <script src="https://unpkg.com/react-dom@18/umd/react-dom.production.min.js"></script>
    <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
    <script src="https://telegram.org/js/telegram-web-app.js"></script>
</head>
<body>
    <div id="root"></div>

    <script type="text/babel">
        const { useState, useEffect } = React;

        // ТУТ ВАШ ОБЪЕКТ workoutData (из предыдущего сообщения)
        const workoutData = {
            warmup: { /* ... данные ... */ },
            main: { /* ... данные ... */ },
            together: { /* ... данные ... */ },
            tips: [ /* ... данные ... */ ]
        };

        function CoupleWorkout() {
            const [activeTab, setActiveTab] = useState("his");
            const [completedExercises, setCompletedExercises] = useState({});

            useEffect(() => {
                if (window.Telegram?.WebApp) {
                    window.Telegram.WebApp.ready();
                    window.Telegram.WebApp.expand();
                }
            }, []);

            // ... Остальная логика вашего компонента (toggleExercise, render и т.д.) ...
            
            return (
                <div style={{ background: "#F9F7F4", minHeight: "100vh" }}>
                    {/* Ваш JSX код здесь */}
                </div>
            );
        }

        const root = ReactDOM.createRoot(document.getElementById('root'));
        root.render(<CoupleWorkout />);
    </script>
</body>
</html>